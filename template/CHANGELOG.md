# Changelog

What each review round changed and why, with the alternatives rejected so they don't get re-litigated. **Not needed to run the plan.** Review reasoning lives here and only here — both earlier packages paid a round for letting it into the operating files.

The reviewer — not the learner — also logs here any structural change made in the runtime after compile (a new column, a new Route value), so the next compile carries it.

| Date | Round | What changed | Rejected | Record |
|---|---|---|---|---|
| | | | | |
